{
  "_comment": "Copy to secrets.json (gitignored) and fill in. Deep-merged over config.json at runtime, so only the secret leaves below live here — everything else stays in config.json. Mirrors the config tree.",
  "notify": {
    "email": {
      "password": ""
    }
  },
  "directory": {
    "m365": {
      "client_secret": ""
    }
  },
  "webui": {
    "password_hash": "",
    "salt": ""
  }
}
